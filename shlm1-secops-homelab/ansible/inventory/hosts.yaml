all:
  children:
    pve_hosts:
      hosts:
        shlm1:
          ansible_host: 10.0.0.24
          ansible_user: ansible
          ansible_ssh_private_key_file: ~/.ssh/shlm1.ansible
    soc_core:
      hosts:
        vsoc1:
          ansible_host: 10.0.0.23
          ansible_user: wzadmin
          ansible_password: "{{ vault_ssh_password }}"
          ansible_become_password: "{{ vault_ssh_password }}"
          ansible_python_interpreter: /usr/bin/python3